# OS
Thumbs.db
desktop.ini
.DS_Store

# Editor
.vscode/
*.code-workspace

# Claude Code local overrides (keep CLAUDE.md tracked, ignore personal ones)
CLAUDE.local.md
.claude/settings.local.json

# Build output, if ever added
dist/
build/

# Accidental npm artifacts from browser-testing tools (this site has no build step)
node_modules/
package.json
package-lock.json

# Never commit
.env
*.pem
*.key
